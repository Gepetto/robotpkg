Fix find qpOASES, as it doesn't ship a .pc

diff --git CMakeLists.txt CMakeLists.txt
index f84689c..739d247 100644
--- CMakeLists.txt
+++ CMakeLists.txt
@@ -93,8 +93,10 @@ SET(Eigen_REQUIRED "eigen3 >= 3.2.4")
 SEARCH_FOR_EIGEN()
 ADD_REQUIRED_DEPENDENCY("hpp-pinocchio >= 4")
 ADD_REQUIRED_DEPENDENCY("hpp-statistics")
+
 IF (${USE_QPOASES})
-  ADD_REQUIRED_DEPENDENCY("qpOASES >= 3.2")
+  SET(QPOASES_PREFIX ${CMAKE_PREFIX_PATH} CACHE PATH "Set the prefix where qpOASES is installed")
+  FIND_LIBRARY(QPOASES_LIBRARY "qpOASES" PATHS ${QPOASES_PREFIX}/lib)
 ENDIF ()

 ADD_SUBDIRECTORY (src)
diff --git src/CMakeLists.txt src/CMakeLists.txt
index 4b1c075..ede6d4a 100644
--- src/CMakeLists.txt
+++ src/CMakeLists.txt
@@ -46,7 +46,7 @@ ADD_LIBRARY(${LIBRARY_NAME}
 PKG_CONFIG_USE_DEPENDENCY(${LIBRARY_NAME} hpp-pinocchio)
 PKG_CONFIG_USE_DEPENDENCY(${LIBRARY_NAME} hpp-statistics)
 IF (${USE_QPOASES})
-  PKG_CONFIG_USE_DEPENDENCY(${LIBRARY_NAME} qpOASES)
+  TARGET_LINK_LIBRARIES(${LIBRARY_NAME} ${QPOASES_LIBRARY})
 ENDIF (${USE_QPOASES})

 INSTALL(TARGETS ${LIBRARY_NAME} DESTINATION lib)
