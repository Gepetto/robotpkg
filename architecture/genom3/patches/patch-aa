Split pkg-config output on newlines too

diff --git pcpp/pragma.c pcpp/pragma.c
index 150d489..d1a7c91 100644
--- pcpp/pragma.c
+++ pcpp/pragma.c
@@ -137,7 +137,7 @@ get_pkgconfig_cflags(char **argv)
 
   /* split cflags at whitespace */
   next = 0;
-  for(p = strtok(cflags, " \t"); p; p = strtok(NULL, " \t")) {
+  for(p = strtok(cflags, " \t\n"); p; p = strtok(NULL, " \t\n")) {
     if (next == 'I') {
       next = 0;
       addpkgidir(p);
