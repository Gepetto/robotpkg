Implicit int - failing with gcc-14

--- src/ope-pxmw.c~	2015-05-27 06:47:05.000000000 +0200
+++ src/ope-pxmw.c	2024-09-10 17:32:26.281138961 +0200
@@ -40,6 +40,9 @@
 #include "ope-pxmw_f.h"
 #include "lang.h"
 
+void _swapshort (char *bp, unsigned n);
+void _swaplong (char *bp, unsigned int n);
+
 /*
  * xwd.c MIT Project Athena, X Window system window raster image dumper.
  *
@@ -61,6 +64,7 @@
  *		  Ghanged the code to adapt it to OPRS.
  */
 
+int
 WritePixmap(Widget  parent, char *file_name, int width, int height, Pixmap pixmap)
 {
      FILE *out_file;
@@ -90,6 +94,7 @@
 
 int format = ZPixmap;
 
+void
 Pixmap_Dump(Widget widget, Pixmap pixmap, int width, int height, FILE   *out)
 {
      Window        window = XtWindow(widget);
@@ -292,9 +297,7 @@
     return(ncolors);
 }
 
-_swapshort (bp, n)
-    register char *bp;
-    register unsigned n;
+void _swapshort (char *bp, unsigned int n)
 {
     register char c;
     register char *ep = bp + n;
@@ -307,9 +310,7 @@
     }
 }
 
-_swaplong (bp, n)
-    register char *bp;
-    register unsigned n;
+void _swaplong (char *bp, unsigned int n)
 {
     register char c;
     register char *ep = bp + n;
