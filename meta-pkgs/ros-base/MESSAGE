In order to use ros packages, you must add the following variables in
your environment:

  ROS_PACKAGE_PATH ${PREFIX}
  ROS_MASTER_URI   http://localhost:11311
  PYTHONPATH       ${PREFIX}/lib/python${PYTHON_VERSION}/site-packages
  PATH             ${PREFIX}/bin
