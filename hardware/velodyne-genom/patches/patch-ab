diff --git codels/velodyneExecTaskCodels.c codels/velodyneExecTaskCodels.c
index 797797b..4f8252a 100644
--- codels/velodyneExecTaskCodels.c
+++ codels/velodyneExecTaskCodels.c
@@ -180,8 +180,10 @@ STATUS
 velodyneExecTaskEnd(void)
 {
   /* Public variables */
-  velodyne_close (interface);
-  fclose (posFileId);
+  if (interface != NULL)
+    velodyne_close (interface);
+  if (posFileId != NULL)
+    fclose (posFileId);
   /* Private variables */
   free (targetTracking);
   /* Posters */
