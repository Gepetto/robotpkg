--- Makefile.in~	2008-04-19 11:08:09.000000000 +0200
+++ Makefile.in	2008-04-19 13:02:20.000000000 +0200
@@ -2,11 +2,11 @@
 
 BOOTSTRAP=	@bootstrap@
 
-.if empty(BOOTSTRAP)
-SUBDIRS=	lib add admin create delete info view audit-packages
-.else
+ifeq (,$(BOOTSTRAP))
+SUBDIRS=	lib add admin create delete info
+else
 SUBDIRS=	lib admin create info
-.endif
+endif
 
 all:
 	@for dir in $(SUBDIRS); do \
